[package]
name = "rifflab-cue"
version = "0.1.0"
edition = "2021"
description = "Cue engine: timeline markers, auto-switch presets, loop regions"

[dependencies]
rifflab-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
log = { workspace = true }
