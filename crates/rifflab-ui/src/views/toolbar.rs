// Toolbar: transport controls, BPM, visual tuner, song selector.
//
// The primary toolbar implementation lives in `rifflab-app/src/main.rs`
// (the toolbar section of `RiffLabApp::update`) since it needs direct access
// to the audio engine and app state.
//
// This module is reserved for future extraction into a reusable widget.
