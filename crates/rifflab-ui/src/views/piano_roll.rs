// Piano roll view: reference notes + played notes, color-coded by accuracy.
//
// The primary piano roll implementation lives in `rifflab-app/src/main.rs`
// (the `draw_piano_roll` function) since it needs direct access to the app's
// scroll/zoom state and the egui Painter.
//
// This module is reserved for future extraction into a reusable widget once
// the interface stabilizes.
